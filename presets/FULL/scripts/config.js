/*
 Configuration script
*/

var config = {};

config.messages = {
	init: 'Script initialized!'
};

config.log = {
	type: true,
	size: true,
	proxy: true,
	connection: true,
	interceptor: false,
	destination: true,
	tags: true,
	buffer: true
};